* IPX/SPX

노벨 네트웨어에서 만든 프로토콜

NDS(Novell Directory Service)에서 사용되었던 프로토콜

NetBIOS와 거의 동일한 서비스를 제공하지만 브로드케스팅 성격의 NetBIOS와는 라우팅이된다는

점에서 차이를 보인다

WIN98출시전까지는 주로 파일서버등에서 강력한 성능을 발휘했다. 하지만 TCP/IP를 등에업은 인터넷이 전세계적으로 광범위하게 보급되는 바람에IPX/SPX는 현재 유명무실해졌다.

* NETBIOS

세션및 전송계층의 서비스를 제공

표준프레임이나 전송을위한 데이터 형식을 제공하지않는다

NETBIOS의 확장판 NETBEUI에서 제공

NetBIOS는 별개의 컴퓨터상에있는 으용프로그램들이 LAN에서 서로 통신할수있게 해주는 프로토콜IBM

Ethernet Token Ring windows 네트워크에 사용

라우팅 지원 안하므로 광역통신망 상에서 통신하는 응용 프로그램들은 반드시 TCP와같은 다른 전송 프로

토콜을 추가 사용해야 한다.

세션모드 내의 에러복구포함

NETBIOS 요구는 메시지 위치와 수신지이름을 다른 것들과 함께 명기하는 네트워크 제어블록 NCB의형식으로 제공

통신모드 - 세션 - 두개의 컴퓨터들이 대화를 위해 연결을 맺을 수 있도록 해줌

커다란 메세지들의 처리와 에러감지및 복구를 제공한다

- 데이터그램 - 비연결형, 메시지의 크기가 작아야 하며 응용프로그램은 에러감지와 복구에 관해 자체적으로 처리해야한다. 근거리 통신망상의 모든 컴퓨터들에게 메시지를 전달하는 기능

고유한 16바이트 주소

* NETBEUI

IBM이 만든 최초의 PC네트워크를 위한 기본적인 네트워크 프로토콜인 NETBIOS의 확장판

NETBIOS는 18개의 명령어에 불과

세션계층은 NETBIOS

트랜스포트 네트워크 -NETBEUI

NETBEUI는 NETBIOS와 마찬가지로 LAN내에서의 통신에 있어 가장 좋은 성능

라우팅 지원안함~~~경로를 정할수 없다는것)

현재가장빠른 프롴토콜중 하나

* AppleT!alk

애플컴퓨터회사개발

저가의 근거리 통신망용프로토콜

애플이외의 컴퓨터 상호간에 통신가능

물리층 데이터링크 종당간데이터흐름층 네임드엔티티층 고신뢰성데이터전성층 최종사용자서비스층

물리층- 애플 독자적인 로컬토크이외에 이더넷과 트큰링을 마련한다.

데이터링크 - 로컬토크에 대응하는 elap토큰링에 대응하는 토큰토크링크엑세스 프로토콜( TLAP) 원격접속용의 ARAP)

종단간데이터흐름 - 데이터전송을위한DDP데타그램 딜리버리 프로토콜)와 루팅용 RTMP라우팅 테이블 메인텐넌스 프로토콜 )등이 사용

최종사용자 서비스 층 AFP애플필링 프로토콜

* TCP

전송계층

상호신뢰있는 연결을 제공하는 프로토콜로서 SLIDING WINDOWS 사용

ACK신호를 사용하여오류없는 데이터 전송을 한다.

데이터 송신전에 경로설정이 되있어야 한다.

TCP헤더 구조

SOURCE PORT 16비트 송신측포트번호

DESTINATION PORT 16 수신측포트번호

SEQUENCE NUMBER 32 순서번횔드 세그먼트된 WINDOW의순서번호로 송수신측사이의 초기연결설정부분에서도사용

ACKOWLEDGE NUMBER 수신측의 데이터오류유뮤 오류시NAK오류메세지 가수신

HEADER LENGTH 4 전체헤더의 길이를 나타내는 필드 32비트의 문자로 표현 헤더의 길이를 지정해주는 필드

RESERVED 16 예약을 위한 필드. 모든 ㅂ트가 0으로 세팅

제어코드 6비트TCP패킷의 종류를 나타내기 위한필드 총 6개의 필드가 있꼬 각각 1비트 씩차지

URG긴급포인터필드의 유효 ACKTCP헤더부분중 ACK번호의 유효 PSH현재대기중모든데타전송하라 RST현재의 접속을 다시설정 SYN양측간의 순서번호를 동기화를 시키기위함 접속설립되었을때사용 FIN 더이상 보낼 데이터가 없다는 의미0

WINDOW SIZE 16 흐름제어 과정에서 사용되는 윈도우 크기를 지정하는 필드

CHECKSUM 16 전체 헤더를 포함한 데이터의 오류검사값을 나타내는필드

URGENT POINTER 16 사용자 응용계층에서 사용하는 필드. 데이터의 깁급을 나타내는 필드

OPTION 0~32 가변적 TCP의 옵션값을 나타내는 필드

PADDING 0~32 가변적 아무의미없는 값을 갖는 필드 OPTION의 길이가 32비트에 미치지 못할 경우 32비트를 마저 채워주어야 하기 떄문 OPTION 과 PADDING이 합하여 32비트가 된다.

* UDP

사용자 데이터그램 프로토콜

전송속도 향상을 위해 흐름제어또는 오류제어를 뺸 프로토콜

송신측은 오로지 송신만을 하는 프로토콜

전송전 연결작업하지않고

SOURCE PORT 16 송신측의 포트번호

DESTINATIOM PORT 16 수신측의 포트번호

MESSAGE LENGTH 16 UDP헤더와 데이터를 포함한 전체길이를 나타냄

CHECKSUM 16 전체헤더를 포함한 데이터의 오류 검사값을 나타내는 필드

* IP

인터넷의 주소체계를 결정하는 프로토콜

호스트의 인터넷 주소를 결정 , 이주소를 사용해 목적지 까지의 경로를 결졍

32비트길이를 가지며 전체 32비트 부분에는 NETWORK ID와 HOST ID 구분

IP헤더

VERSION 4 버전을 나타내는 필드

HEADER LENGTH 4 헤더의 전체 길이르 ㄹ나타내는 필드 옵션 옵션사용안하면20바이트 옵션사용시 더늘어남

TOS 8 데이터의 처리 방법을 나타내는 필드 ,현재QOS에서 사용방법을 채택

PACKETY LENGTH 16 헤더를 포함한 전체 데이터 그램의 길이를 나타내는 필드 최대 65535바이트

IDENTIFICATION 16 세분화된 데이터그램을 수신측에서 다시 재조림을 할수 있도록 송신측에서 정한 번호가 들어있다. 중복번호 안된

FLAG 3 데이터 그램이 세분화 되었을때 제어 정보가 들어가는 필드

FRAGMENTATION 전체 데이터그램의 크기를 알려주는 필드

TTL 네트워크상에서 생존할수있는 시간을 지정해주는 필드

전송PROTOCOL상위 TRANSPORT계층의 프로토콜을 지정하는 필드 TCP 6 UDP17로 기록

HEADER CHECKSUM 16 데이터그램의 빠른처리를 위해 IP헤더부분만 에러검사갑을 넣는 필드

SENDER ADDRESS & DESTINATION ADDRESS 32 송수신측의 IP주소를 나타내는 필드 32비트

OPTION 0 또는 32 IP헤더의 옵션을 나타내는 필드 , 잘사용안함 특수목적으로 많이 사용
