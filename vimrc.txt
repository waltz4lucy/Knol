set nocompatible                  " Must come first because it changes other options.
set encoding=utf-8                " Vim에서 사용할 encoding
set fileencoding=utf-8            " 현재 파일에 사용할 encoding
set fileencodings=utf-8,cp949,cp932,euc-jp,shift-jis,big5,ucs-2le,latin1 " 파일을 열때, 자동적으로 적용할 encoding의 순서
set termencoding=utf-8            " terminal에서 적용할 encoding
set t_Co=256                      " 256 Color
hi Pmenu ctermbg=238 gui=bold
"runtime syntax/colortest.vim
set mouse=a
set showcmd                       " Display incomplete commands.
set showmode                      " Display the mode you're in.
set showmatch                     " DIsplay the match
set backspace=indent,eol,start    " Intuitive backspacing.
set hidden                        " Handle multiple buffers better.
set wildmenu                      " Enhanced command line completion.
set wildmode=list:longest         " Complete files like a shell.
set ignorecase                    " Case-insensitive searching.
set smartcase                     " But case-sensitive if expression contains a capital letter.
set number                        " Show line numbers.
set nuw=1
set ruler                         " Show cursor position.
set incsearch                     " Highlight matches as you type.
set hlsearch                      " Highlight matches.
set nowrap                        " Turn on line wrapping.
set scrolloff=2                   " Show 3 lines of context around the cursor.
set title                         " Set the terminal's title
set titlestring=%t%(\ %M%)
set nobackup                      " Don't make a backup before overwriting a file.
set nowritebackup                 " And again.
set directory=$HOME/.vim/tmp//,.  " Keep swap files in one location
set sol                           " Start Line First
set magic
set autoindent
set smartindent
set tabstop=2                     " Global tab width.
set shiftwidth=2                  " And again, related.
set expandtab                     " Use spaces instead of tabs
set laststatus=2                  " Show the status line all the time
set statusline=%<%F%h%m%r%h%w%y\ %=\ col:%c%V\ ascii:%b\ pos:%o\ lin:%l\,%L\ %P
set history=15
set paste                         " Paste
set noai                          " Paste No Ai
"filetype plugin indent on         " Turn on file type detection. Paste Issue
syntax on                         " Turn on syntax highlighting.

"let g:neocomplcache_enable_at_startup = 1

"map .# :s/^\(\s*\)/\1# <CR>
"map .3 :s/^\(\s*\)# \1/<CR>
"map .- :s/^\(\s*\)/\1-- <CR>
"map ._ :s/^\(\s*\)-- \1/<CR>
"map ./ :s/^\(\s*\)/\1\/\/ <CR>
"map .? :s/^\(\s*\)\/\/ \1/<CR>

if has("autocmd")
    autocmd FileType lua          map <F12> :w<CR>:!moai main.lua<CR>:!moai %<CR>

    " 괄호 자동 완성
    autocmd FileType *            inoremap { {}<LEFT>
    autocmd FileType *            inoremap [ []<LEFT>
    autocmd FileType *            inoremap ( ()<LEFT>
    autocmd FileType c,cpp,php    inoremap { {<CR>}<C-O>O
    autocmd FileType c,cpp,php    inoremap " ""<LEFT>
    autocmd FileType c,cpp,php    inoremap ' ''<LEFT>

    autocmd FileType *            set nocindent
    autocmd FileType c,cpp,php    set cindent
    autocmd FileType lua          set complete+=k~/.vim/syntax/lua.vim isk+=.,(
endif
